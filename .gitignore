# extension
.output
.wxt

node_modules
.env
.env.local
.DS_Store

# Kept locally only as migration reference; not part of this extension.
legacy-submit-agent/

# Private growth catalogue and machine-local notes
local/
extension-dist/
